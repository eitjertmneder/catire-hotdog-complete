import React from 'react'; import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';  interface DrawerItemProps {   icon: string;   label: string;   onPress: () => void;   active?: boolean; }  export const DrawerItem: React.FC<DrawerItemProps> = ({ icon, label, onPress, active }) => {   return (     <TouchableOpacity style={[styles.container, active && styles.active]} onPress={onPress}>       <Text style={styles.icon}>{icon}</Text>       <Text style={[styles.label, active && styles.activeLabel]}>{label}</Text>     </TouchableOpacity>   ); };  const styles = StyleSheet.create({   container: {     flexDirection: 'row',     alignItems: 'center',     paddingVertical: 12,     paddingHorizontal: 16,     borderRadius: 8,   },   active: {     backgroundColor: '#FEE2E2',   },   icon: {     fontSize: 20,     marginRight: 12,   },   label: {     fontSize: 16,     color: '#212121',   },   activeLabel: {     color: '#D32F2F',     fontWeight: '600',   }, });